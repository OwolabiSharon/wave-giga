const rideConfig = {
    feePerKm: {
      luxury: 0.3,
      regular: 0.2,
      premium: 0.25,
      // Add more ride types and their corresponding fees here
    } as Record<string, number>,
  };

export default 
    rideConfig
